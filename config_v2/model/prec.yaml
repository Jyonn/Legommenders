name: PREC
config:
  use_news_content: true
  neg_count: 4
  hidden_size: 64
  news_config:
    hidden_size: ${model.config.hidden_size}$
    num_attention_heads: 4
    num_hidden_layers: 3
    inputer_config:
      use_cls_token: false
      use_sep_token: true
  user_config:
    hidden_size: ${model.config.hidden_size}$
    num_attention_heads: 4
    num_hidden_layers: 3
    inputer_config:
      use_cls_token: false
      use_sep_token: false
