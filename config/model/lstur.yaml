$$import:
  - common/operators/user-gru.yaml
  - common/predictors/dot.yaml
name: LSTUR
meta:
  item: CNNCat
config:
  use_fast_eval: ${fast_eval:true}$
  use_item_content: true
  hidden_size: ${hidden_size}$
  item_hidden_size: ${item_hidden_size}$
  item_config:
    dropout: 0.1
    kernel_size: 3
